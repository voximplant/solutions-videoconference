import { mockGrid, TileInterface } from '@/helpers/layouts/index';

const data: TileInterface = {
  default: mockGrid,
  defaultScreen: mockGrid,
  large: mockGrid,
  largeScreen: mockGrid,
  mobile: mockGrid,
  mobileScreen: mockGrid,
};
